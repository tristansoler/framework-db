class CoreDataProcess:
    __init__:
        if config.XXX == LAMBDA
            self.data_process = pandas
        else:
            self.data_process = spark

def merge(self, dataFrame: any, flowdata: str, table_name: str):
    self.data_process.merge(dataFrame, flowdata, table_name)

def merge(dataframe: Dataframe, ...):